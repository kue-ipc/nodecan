# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  network: one
  address: 
  netmask: 1
  gateway: 
  use_dhcp: false
  dhcp_type: one

two:
  network: two
  address: 
  netmask: 1
  gateway: 
  use_dhcp: false
  dhcp_type: two
