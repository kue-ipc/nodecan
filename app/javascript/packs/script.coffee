import 'bootstrap'

# import React from 'react'
#
# IPPool = ({id, first, last, size, pattern}) ->
#   <div className="form-group">
#     <input type="text" className="form-control" maxlength={size} size={size} pattern={pattern}
#         name={"network[ipv4_network][pools][#{id}][first]"}
#         id={"network_ipv4_network_pools_#{id}_first"}/>
#     -
#     <input type="text" className="form-control" maxlength={size} size={size} pattern={pattern}
#         name={"network[ipv4_network][pools][#{id}][last]"}
#         id={"network_ipv4_network_pools_#{id}_last"}/>
#   </div>
#
# IPPoolList = (list) ->
#   list.map (pool)
#   <div>
#   </div>


# import jQuery from 'jquery'
# window.$ = jQuery
# console.log($)
# import 'popper.js'
# import Bootbox from 'bootbox'
# @Bootbox = Bootbox
# console.log(@)
# window.Bootbox = Bootbox
#
# console.log(global)
# console.log(window.confirm)
# console.log('hello')
#
# window.confirm = (message) ->
#   console.log(message)
#   false
