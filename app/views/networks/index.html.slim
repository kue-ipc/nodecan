h1 Listing networks

table
  thead
    tr
      th Name
      th Display name
      th Vlan
      th Use ipv4
      th Use ipv6
      th Use auth
      th Note
      th
      th
      th

  tbody
    - @networks.each do |network|
      tr
        td = network.name
        td = network.display_name
        td = network.vlan
        td = network.use_ipv4
        td = network.use_ipv6
        td = network.use_auth
        td = network.note
        td = link_to 'Show', network
        td = link_to 'Edit', edit_network_path(network)
        td = link_to 'Destroy', network, data: { confirm: 'Are you sure?' }, method: :delete

br

= link_to 'New Network', new_network_path
